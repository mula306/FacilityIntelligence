# T01 Admin Shell, Auth, RBAC, Audit Base

- Status: `completed`
- Wave: `B`
- Owners: `Frontend`, `Backend`

## Scope

Deliver the admin shell, local sign-in placeholder, role-aware route guards, user and role views, and audit event capture plus viewing.

## Write Boundary

- `packages/ui`
- `apps/api/src/modules/auth`
- `apps/api/src/modules/admin`
- `apps/web/src/features/auth`
- `apps/web/src/features/admin`

## Dependencies

- T00 completed

## Acceptance

- Local sign-in returns authenticated user context
- Role gates control admin pages
- Audit events are persisted and visible in the UI
