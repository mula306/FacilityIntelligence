# T06 Network and Connectivity

- Status: `completed`
- Wave: `C`
- Owners: `Backend`, `Frontend`

## Scope

Network profiles, circuits, access points, measured speeds, and connectivity observations.

Current implementation inside the track boundary uses `network:read` and `network:write` for module assumptions. API routes, service logic, and UI workflow pages are implemented in the module directories, while shared app shell wiring remains for the main agent.
Current implementation inside the track boundary uses `network:read` and `network:write` for module assumptions. API routes, service logic, and UI workflow pages are implemented in the module directories, and shared app shell wiring is complete.

## Dependencies

- T02 completed
- Track boundary implementation delivered for module internals

## Acceptance

- Network service and measured performance can be recorded and queried by facility
- Access points tie back to location hierarchy
- Archive routes, archive-only status transitions, and profile-to-facility validation are hardened after integration
