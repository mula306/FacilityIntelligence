# T09 Coverage Analysis and Visualization

- Status: `completed`
- Wave: `F`
- Owners: `Wi-Fi`, `Jobs`, `Frontend`
- Contract state: `frozen after shared coverage contract update`

## Scope

- Coverage aggregation from persisted Wi-Fi scan samples
- Facility, floor, zone, and room quality summaries with persisted coverage assessments
- Floor-level coverage overlay using spatial room and zone context plus sample heat points
- Dead-zone annotation rebuild workflow tied to floor plans and map annotations
- Coverage review workspace inside the shared admin shell

## Delivered

- Shared coverage contracts for aggregation commands and dead-zone annotation rebuild commands
- Prisma schema and migration hardening for `CoverageAssessment` scope, metrics, and location relations
- Jobs-layer aggregation utility for coverage bands, scores, heat points, and dead-zone drafts
- API repository, service, routes, persistence, and audit emission for coverage bootstrap, aggregation, and annotation rebuild
- Web coverage workspace with floor-context selection, assessment review tables, dead-zone review, and floor overlay visualization
- Seeded baseline T09 data including persisted assessments and a coverage-gap annotation for local bootstrap review
- Admin shell route and navigation entry gated by `coverage:read` and `coverage:write`

## Multi-Agent Delivery Record

- Backend/jobs worker owned `apps/api/src/modules/coverage` and `packages/jobs/src`
- Frontend worker owned `apps/web/src/features/coverage`
- Main agent owned shared contracts, schema, migration, seed data, permission wiring, shell integration, reconciliation, and QA verification
- Post-worker integration corrected permission scope, route-path alignment, bootstrap payload shape, and admin-shell wiring before merge

## Verification

- `npm install`
- `npm run build`
- `npm test`
- `npm run db:seed`

## Handoff to T10

- Reuse persisted `CoverageAssessment` records and coverage scores as direct inputs into readiness and risk scoring
- Keep T10 drill-downs traceable from readiness and risk summaries back to coverage assessments, Wi-Fi sessions, and raw scan samples
- Preserve the current repository and jobs boundary so future reporting or scheduled aggregation can move off synchronous flows without contract churn
