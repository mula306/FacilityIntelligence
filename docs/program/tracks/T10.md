# T10 Risk, Readiness, Reporting

- Status: `completed`
- Wave: `G`
- Owners: `Backend`, `Frontend`, `Jobs`
- Contract state: `frozen after shared readiness contract update`

## Scope

- Incident tracking tied to facility, building, floor, zone, and room scope
- Manual risk item workflows plus system-generated risk item reporting
- Deterministic readiness scoring using T05 inventory, T06 connectivity, and T09 coverage inputs
- Readiness drill-downs and operational reporting inside the shared admin shell
- Seeded baseline data for immediate local review of the readiness workspace

## Delivered

- Shared readiness contracts for incidents, risk items, and recalculation commands
- Prisma schema and migration support for incident, risk item, and readiness score metadata, indexes, and scoring details
- Jobs-layer readiness calculation with deterministic score inputs and derived risk generation
- API repository, service, routes, and audit emission for readiness bootstrap, incidents, manual risk items, and recalculation
- Web readiness workspace with scope selection, score review, incident management, manual risk item management, and operational reporting
- Admin shell route and navigation entry gated by `readiness:read` and `readiness:write`
- Seeded triage readiness baseline including an active incident, manual risk item, system-generated risk item, and floor-scoped readiness score

## Multi-Agent Delivery Record

- Backend worker owned `apps/api/src/modules/readiness`
- Frontend worker owned `apps/web/src/features/readiness`
- Main agent owned shared contracts, schema, migration, permissions, shell integration, seed data, reconciliation, and QA verification
- Post-worker integration corrected scope-aware UI refresh behavior, manual-risk form constraints, score detail rendering, route wiring, and creation-response hydration before merge

## Verification

- `npm run build`
- `npm test`
- `npm run db:seed`

## Handoff to T11

- Harden optional location foreign keys into explicit Prisma relations for readiness-era models so enrichment can move from lookup joins to relation-safe ORM access
- Keep import/export and reporting work aligned to the frozen readiness contracts instead of introducing parallel reporting payloads
- Use T11 to review indexing, SQL portability, and import surfaces for incidents, risk items, and readiness score history
