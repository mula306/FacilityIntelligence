# T08 Wi-Fi Scan Capture

- Status: `completed`
- Wave: `E`
- Owners: `Wi-Fi`, `Backend`, `Frontend`
- Contract state: `frozen after shared Wi-Fi contract update`

## Scope

- Wi-Fi scan session management with facility, building, floor, zone, and room context
- Structured Wi-Fi scan sample capture with timestamp, band, RSSI, BSSID, SSID, and floor-map coordinates
- Collector and device metadata capture for scan sessions
- CSV-assisted sample import on the same telemetry schema used for manual capture
- Floor preview and scan-point visualization inside the admin shell workflow

## Delivered

- Shared Wi-Fi contracts for scan-session input, scan-sample input, and CSV-assisted sample import payloads
- Prisma index hardening for `WifiScanSession` and `WifiScanSample` query paths
- Seed data for baseline access-point and Wi-Fi scan telemetry records
- API repository, service, routes, import workflow, archive workflow, and audit emission for Wi-Fi scan capture
- Web Wi-Fi workspace with floor-context selection, session management, sample management, CSV import, and floor preview canvas
- Admin shell route and navigation entry gated by `wifi:read` and `wifi:write`

## Multi-Agent Delivery Record

- Backend worker owned `apps/api/src/modules/wifi`
- Frontend worker owned `apps/web/src/features/wifi`
- Main agent owned shared contracts, schema indexes, migration, seed data, shell wiring, reconciliation, and QA verification
- Post-worker integration corrected frontend/backend field drift and completed the missing route and test layer before merge

## Verification

- `npm run db:migrate -- --name add_wifi_scan_indexes_t08`
- `npm run build`
- `npm test`
- `npm run db:seed`

## Handoff to T09

- Reuse the T08 session and sample schema as the raw telemetry source for floor, room, and facility coverage aggregation
- Build coverage jobs and overlays on the frozen floor-context and coordinate model from T07 and T08 rather than reopening shared contracts
- Keep summary generation idempotent so room-level and floor-level coverage assessments remain traceable back to raw scan samples
